using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using DG.Tweening;
using TMPro;

/*public class DoTText : MonoBehaviour
{
    Text a_text;
    public static void DoText(this TextMeshProUGUI a_text, float duration)
    {
        
        a_text.maxVisibleCharacters = 0;
        DOTween.To(x => a_text.maxVisibleCharacters = (int)x, 0f, a_text.text.Length, 2f);
    }
}

}*/
